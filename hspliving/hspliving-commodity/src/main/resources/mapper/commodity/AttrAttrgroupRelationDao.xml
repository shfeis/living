<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hspedu.hspliving.commodity.dao.AttrAttrgroupRelationDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.hspedu.hspliving.commodity.entity.AttrAttrgroupRelationEntity" id="attrAttrgroupRelationMap">
        <result property="id" column="id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrGroupId" column="attr_group_id"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>

    <!--批量删除 属性-属性组关联关系 的方法,id指定要实现的DAO方法-->
    <delete id="deleteBatchRelation">
        DELETE
        FROM commodity_attr_attrgroup_relation
        WHERE
        <foreach collection="entities" item="entity" separator="OR">
            (attr_group_id = #{entity.attrGroupId} AND attr_id=#{entity.attrId})
        </foreach>
    </delete>

</mapper>